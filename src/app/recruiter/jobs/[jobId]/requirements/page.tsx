import { prisma } from "@/lib/prisma";
import { requirePageUser } from "@/lib/auth/page";
import { RequirementEditor } from "@/components/RequirementEditor";
import { notFound } from "next/navigation";
export default async function RequirementsPage({params}:{params:Promise<{jobId:string}>}){const u=await requirePageUser(["RECRUITER","ADMIN"]);const{jobId}=await params;const job=await prisma.job.findUnique({where:{id:jobId},include:{requirements:{orderBy:{createdAt:"asc"}}}});if(!job|| (u.role!=="ADMIN"&&job.companyId!==u.companyId)) notFound();return <main className="mx-auto max-w-[1440px] px-4 py-6 sm:px-6 lg:px-8 lg:py-8"><div className="page-header"><div><p className="page-eyebrow">{job.title}</p><h1 className="page-title">Approve evaluation criteria</h1><p className="page-description">Suggested criteria remain inactive until recruiter approval. Fit and coverage calculations only use the role definition you approve.</p></div></div><div className="mt-6"><RequirementEditor jobId={job.id} initial={job.requirements} status={job.status}/></div></main>}
