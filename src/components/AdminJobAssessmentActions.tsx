"use client";
import { useRouter } from "next/navigation";
export function AdminJobStatus({ jobId, status }: { jobId: string; status: string }) { const router=useRouter(); async function setStatus(next:string){await fetch(`/api/admin/jobs/${jobId}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:next})});router.refresh();} return <select value={status} onChange={(e)=>setStatus(e.target.value)} className="rounded-lg border border-zinc-300 bg-white p-2 text-xs"><option>DRAFT</option><option>OPEN</option><option>CLOSED</option></select>; }
export function AdminAssessmentToggle({ assessmentId, active }: { assessmentId: string; active: boolean }) { const router=useRouter(); async function toggle(){await fetch(`/api/admin/assessments/${assessmentId}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({active:!active})});router.refresh();} return <button onClick={toggle} className="rounded-lg border border-zinc-300 px-3 py-2 text-xs font-medium">{active?"Deactivate":"Activate"}</button>; }
