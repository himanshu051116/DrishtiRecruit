import {z} from "zod"; import {EvidenceSourceType,EvidenceStrength,RequirementCategory,RequirementPriority,VerificationMethod} from "@/domain/enums";
export const JobRequirementDraftSchema=z.object({name:z.string().min(1).max(120),description:z.string().max(1000).optional(),category:z.nativeEnum(RequirementCategory),priority:z.nativeEnum(RequirementPriority),weight:z.number().min(0).max(1),minimumEvidenceLevel:z.nativeEnum(EvidenceStrength),verificationRequired:z.boolean()});
export const EvidenceCreateSchema=z.object({requirementId:z.string(),sourceType:z.nativeEnum(EvidenceSourceType),sourceId:z.string().optional(),sourceExcerpt:z.string().max(4000).optional(),strength:z.nativeEnum(EvidenceStrength),confidence:z.number().min(0).max(1),supportsRequirement:z.boolean(),contradictsRequirement:z.boolean(),verified:z.boolean().default(false)});
export const VerificationApprovalSchema=z.object({method:z.nativeEnum(VerificationMethod),templateId:z.string().optional(),approved:z.boolean()});
