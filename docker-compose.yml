services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: drishtirecruit
    ports:
      - "5432:5432"
    volumes:
      - drishtirecruit_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d drishtirecruit"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  drishtirecruit_pgdata:
